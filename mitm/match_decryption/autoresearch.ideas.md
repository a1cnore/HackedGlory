- `1011` appears exactly 6 times in full matches and likely carries per-player spawn descriptors. Its first 4 bytes repeat across matches in a way that looks hero/skin-specific, and its first 8 bytes match `1006[164:172]`. Build a clustering pass around that shared ID to recover a stable `hero_type_id` per player, then look for a way to map those IDs to `1107` hero strings.
- Compare corrected `1011` spawn stats by `hero_type_id`; the pair at offsets `138:142` / `142:146` looks like spawn health/max-health and may fingerprint heroes when combined with scalar streams.
- `1052` common type bytes and value ranges should be nameable by correlation: e.g. constant 400/100/1.5-like values may be range/resource/multiplier stats. Cross-check against `hero_type_id` clusters.
- `1087` likely carries inventory/loadout-like state. The first code burst per player has repeated values like `121, 122, 123, 173, 174, 254, 255, 273`; classify which are structural markers vs actual item/property IDs, then turn changes into item-buy/state events.
- Snapshot follow-up: in the current corpus, full-match `1113`/`1114` bytes `56:104` look static for active players, and the changing words around `16:24` mostly settle during the early setup burst instead of acting like late-game scoreboard counters. If snapshots still hide useful semantics, focus on those early-changing setup words or item/loadout fingerprints rather than expecting a direct live CS/kills field there.
- Post-death reward follow-up: after the current `0x3e/0x4d` kill fallback, a second pass over unique opponent-only `1086` type `0x40` deltas improves kill coverage a lot in offline simulation, but it adds too many kills to trust blindly. Revisit it only with an extra corroboration signal (e.g. nearby victim-targeted `1087`, damage spike, or source-side followup) so we do not overfit ambient post-death pulses.
- CS follow-up: `1087` farm targets `2000/2008` are reused channel-like ids, and the paired `1087 -> 1086` bursts fire roughly every `0.5-0.6s` for active farmers. Shortening the current 3.0s dedupe overcounts badly, so the next angle should be burst structure / delta-size classification inside those farm windows, not a looser time gate.
- CS gameplay constraint from user validation: lane minion death should produce exactly one CS holder (the last-hitter) plus at most one same-team ambient gold recipient. Broad symmetric reward pulses where all 3 teammates—or even both full teams—get identical `1086` gains are almost certainly ambient / periodic gold noise, not minion kills. Use that to reject fake farm bursts before trying shorter dedupe windows.
